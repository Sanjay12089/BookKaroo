# Never lock platform-specific optional binaries into the lock file.
# This prevents Windows dev machines from committing win32-specific
# packages (e.g. @tailwindcss/oxide-win32-x64-msvc) that break
# Linux CI/Vercel deployments.
optional=true
